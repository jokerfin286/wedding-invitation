import {useEffect,useRef,useState} from 'react';import {Pause,Play,Music2} from 'lucide-react';
export default function MusicPlayer({music}){const audio=useRef(null),[playing,setPlaying]=useState(false);useEffect(()=>()=>audio.current?.pause(),[]);if(!music.enabled)return null;const toggle=async()=>{if(!audio.current)return;if(playing){audio.current.pause();setPlaying(false)}else{try{await audio.current.play();setPlaying(true)}catch{setPlaying(false)}}};return <><audio ref={audio} src={music.src} loop preload="none" onEnded={()=>setPlaying(false)}/><button className="music" onClick={toggle} aria-label={playing?'Пауза':'Включить музыку'}><span className="music__icon">{playing?<Pause size={15}/>:<Play size={15}/>}</span><span>{playing?'Музыка играет':'Включите музыку'}</span><Music2 size={16}/></button></>}
