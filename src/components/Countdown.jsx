import {useEffect,useState} from 'react';import Reveal from './Reveal';import {getCountdown,pad} from '../utils/countdown';
export default function Countdown({w}){const [time,setTime]=useState(()=>getCountdown(w.weddingDate));useEffect(()=>{const id=setInterval(()=>setTime(getCountdown(w.weddingDate)),1000);return()=>clearInterval(id)},[w.weddingDate]);return <section className="section countdown"><Reveal><span className="eyebrow">Дата</span><div className="date-big">{w.dateText}</div><p className="muted">{time?'До свадьбы осталось':'Сегодня наш день!'}</p>{time&&<div className="count-grid">{[['days','дней'],['hours','часов'],['minutes','минут'],['seconds','секунд']].map(([k,l])=><div className="count" key={k}><strong>{pad(time[k])}</strong><span>{l}</span></div>)}</div>}</Reveal></section>}
